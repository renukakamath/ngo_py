pragma solidity >= 0.5.0 < 0.7.0;
contract medicines{
 
struct donation{

	// State variables
	uint mid;
	uint m_id;
	uint w_id;
	string amount;
	string date;

}

 
donation []m;



function add_payment(uint mid, uint u_id, uint w_id,string memory amount,string memory date) public{
	donation memory m1=donation(mid,u_id,w_id,amount,date);
	m.push(m1);
}


}
